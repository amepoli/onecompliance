{
    "$schema": "../views.schema.json",
    "inheritsFrom": "sondaggi",
     "entryKey": "sondaggi_closed",
    "predefinedQueries": [
        {
            "operation": "selectTable",
            "queryString": "select * from entrasp.sondaggi where codice_azienda=$codice_azienda$ and stato='C'",
            "type": "main"
        }
    ]
}