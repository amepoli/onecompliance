{
    "$schema": "../views.schema.json",
    "entryKey": "sondaggi_sub_of_scadenziario",
    "inheritsFrom": "sondaggi",
    "define": {
        "mask_condition": "snd.id_sondaggio IN (SELECT id_sondaggio FROM entrasp.compiti WHERE (codice_compito_parent=$codice_compito$ OR codice_compito=$codice_compito$)AND codice_azienda=$codice_azienda$)"
    }
}