{
	"$schema": "../views.schema.json",
	"entryKey": "ferie",
	"inheritsFrom": "mie_ferie",
	"predefinedQueries": [
		{
			"operation": "selectTable",
			"type": "main",
			"queryString": "select * from entrasp.festivita where dynamo_user is not null and dynamo_user in(select distinct dynamo_user from entrasp.employers em where codice_azienda in (select codice_azienda from entrasp.employers em2 where dynamo_user='€global_username€')"
		}
	]
}