{
    "$schema": "../views.schema.json",
    "entryKey": "sondaggi_sub_of_eventi_calendario",
    "inheritsFrom": "sondaggi",
    "define": {
        "mask_condition": "snd.id_sondaggio IN (SELECT id_sondaggio FROM entrasp.eventi WHERE (id_evento_parent=$id_evento$ OR id_evento=$id_evento$))"
    }
}