{
  "$schema": "../views.schema.json",
  "inheritsFrom": "domande_dettaglio",
  "entryKey": "domande_ISO27001",
  "exportQueries": {
    "tableQueries": [
      {
        "queryString": "select c1.* from entrasp.controlli c1 inner join (select max(data_riferimento) as maxdata from entrasp.controlli where codice_azienda=$codice_azienda$) c2 on c1.data_riferimento=c2.maxdata where c1.codice_azienda=$codice_azienda$",
        "label": "Conteggio controlli complessivi"
      }
    ]
  }
}